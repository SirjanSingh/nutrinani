# === AWS Cognito (required) ===
VITE_COGNITO_REGION=ap-southeast-2
VITE_COGNITO_USER_POOL_ID=ap-southeast-2_cT3TFrC3N
VITE_COGNITO_USER_POOL_CLIENT_ID=1l8uccafo17v8925em6tm37h0l

# === Cognito Hosted UI / Google OAuth (required for Google Sign-In) ===
VITE_COGNITO_DOMAIN=ap-southeast-2ct3tfrc3n.auth.ap-southeast-2.amazoncognito.com

# Leave these UNSET on Vercel. The app will auto-use window.location.origin,
# which works for every Vercel preview + production URL out of the box.
# Only set them if you need to pin to one exact URL.
# VITE_COGNITO_REDIRECT_SIGN_IN=https://your-domain.vercel.app/
# VITE_COGNITO_REDIRECT_SIGN_OUT=https://your-domain.vercel.app/

# === Google OAuth ===
VITE_GOOGLE_CLIENT_ID=1001817854202-de37d7vel9vkhi5d1u1fvs07mnpuj3hu.apps.googleusercontent.com

# === App Environment ===
VITE_APP_ENV=production

# === Backend API (AWS API Gateway) ===
VITE_API_BASE_URL=https://tfn02c762l.execute-api.ap-southeast-2.amazonaws.com
VITE_API_TIMEOUT=10000

# === Feature Flags ===
VITE_ENABLE_GOOGLE_AUTH=true
VITE_ENABLE_EMAIL_AUTH=true
VITE_ENABLE_DEMO_MODE=false
